# 页面生成器使用说明

## 🚀 快速生成详情页

### 方法一：使用Web界面生成器（推荐）

1. 打开 `generator.html` 文件
2. 在输入框中输入事件ID（如：`tang`, `qin`, `han`）
3. 点击"生成页面"按钮
4. 查看生成的HTML代码
5. 点击"下载HTML文件"保存

### 方法二：使用JavaScript API

```javascript
// 在浏览器控制台中使用
const generator = new PageGenerator();
await generator.loadTemplate();

// 从事件ID生成
const html = await generator.generateFromEventId('tang');
console.log(html);

// 下载文件
await generator.generateAndDownload('tang', 'detail_tang.html');
```

## 📋 工作流程

### 1. 规划网页模板 ✅
- 已创建标准化模板：`templates/detail-standard.html`
- 使用占位符变量：`{{TITLE}}`, `{{INTRO}}`, `{{SECTION1_TITLE}}` 等

### 2. 搜索历史内容 ✅
- 历史事件数据：`data/history.json`
- 内容模板库：`data/content-templates.json`

### 3. 生成历史内容 ✅
- 内容模板库包含：
  - `intro`: 序章介绍
  - `section1/2/3`: 三个核心叙事章节
  - `examPoints`: 考点解析
  - `impacts`: 历史回响

### 4. 填充模板 ✅
- `PageGenerator` 类自动：
  - 加载模板
  - 合并数据
  - 替换占位符
  - 生成知识图谱和核心词列表

### 5. 生成网页 ✅
- 输出完整的HTML文件
- 可直接使用或进一步编辑

## 📝 添加新内容模板

在 `data/content-templates.json` 中添加新模板：

```json
{
  "templates": {
    "新事件ID": {
      "intro": "序章内容...",
      "section1": {
        "title": "章节1标题",
        "icon": "crown",
        "content": "<div>HTML内容...</div>"
      },
      "examPoints": [
        {
          "title": "【必考点1】",
          "content": "考点内容..."
        }
      ],
      "impacts": [
        {
          "title": "01 影响1",
          "content": "影响内容..."
        }
      ]
    }
  }
}
```

## 🎯 已完成的模板

- ✅ 秦朝 (qin)
- ✅ 汉朝 (han)  
- ✅ 唐朝 (tang)

## 💡 优势

1. **速度快**：模板化生成，秒级完成
2. **标准化**：统一格式，易于维护
3. **可扩展**：轻松添加新事件
4. **自动化**：减少重复工作

## 🔧 技术栈

- 原生JavaScript（无依赖）
- JSON数据驱动
- 模板替换机制
- 浏览器端生成

---

**使用提示**：打开 `generator.html` 即可开始快速生成详情页！
